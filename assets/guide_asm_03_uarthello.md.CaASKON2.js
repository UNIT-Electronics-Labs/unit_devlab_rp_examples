import{_ as a,o as s,c as n,a2 as i}from"./chunks/framework.HMsj64NP.js";const h=JSON.parse('{"title":"Ensamblador: Hello World por USB","description":"","frontmatter":{},"headers":[],"relativePath":"guide/asm/03_uarthello.md","filePath":"guide/asm/03_uarthello.md"}'),o={name:"guide/asm/03_uarthello.md"};function l(t,e,r,d,c,p){return s(),n("div",null,[...e[0]||(e[0]=[i(`<h1 id="ensamblador-hello-world-por-usb" tabindex="-1">Ensamblador: Hello World por USB <a class="header-anchor" href="#ensamblador-hello-world-por-usb" aria-label="Permalink to &quot;Ensamblador: Hello World por USB&quot;">​</a></h1><p>Esta práctica introduce la capa de ensamblador ARM Thumb del curso mediante el ejemplo más simple posible: enviar una cadena de texto por el puerto USB integrado del RP2040. A diferencia de las prácticas de la sección &quot;Periféricos Básicos&quot;, aquí el código en <code>main.S</code> se reduce a un arranque que invoca, mediante instrucciones de ensamblador explícitas, a funciones ya compiladas del Pico C/C++ SDK. El objetivo no es reemplazar al SDK, sino mostrar de forma directa cómo una llamada de alto nivel como <code>printf</code> se traduce, en última instancia, en una secuencia de instrucciones Thumb con paso de parámetros por registros (<code>r0</code>, <code>r1</code>, ...) y una instruccion de rama con enlace (<code>bl</code>).</p><h2 id="concepto-teorico" tabindex="-1">Concepto Teórico <a class="header-anchor" href="#concepto-teorico" aria-label="Permalink to &quot;Concepto Teórico&quot;">​</a></h2><p>El procesador del RP2040 es un ARM Cortex-M0+, que implementa el conjunto de instrucciones <strong>ARMv6-M Thumb</strong>. Este conjunto está optimizado para densidad de código: la mayoría de las instrucciones ocupan 16 bits, con un subconjunto reducido de instrucciones de 32 bits (entre ellas, <code>BL</code>, usada para llamadas a subrutina).</p><p>Dos elementos son centrales para entender el código de esta práctica:</p><ul><li><strong>Convención de paso de parámetros (AAPCS):</strong> al llamar a una función, los primeros argumentos se colocan en los registros <code>r0</code>, <code>r1</code>, <code>r2</code> y <code>r3</code>, en ese orden. El valor de retorno, si existe, queda en <code>r0</code>. Esta convención es la que permite que una instrucción en ensamblador invoque directamente una función escrita en C, siempre que se respete el orden y el tipo de los argumentos.</li><li><strong>Instrucción <code>BL</code> (Branch with Link):</strong> salta a la dirección de una subrutina y guarda la dirección de retorno en el registro de enlace (<code>LR</code>), permitiendo que la subrutina, al terminar, regrese al punto exacto desde donde fue llamada.</li></ul><p>En el caso particular del USB, la inicialización y comunicación por este periférico requieren un stack de protocolo completo (enumeración del dispositivo, descriptores USB, gestión de endpoints según la clase CDC-ACM). Esta lógica está implementada en TinyUSB, integrado en el Pico C/C++ SDK, y no resulta práctico ni pedagógicamente razonable reproducirla en ensamblador puro. Por esta razón, el ensamblador de esta práctica se limita a invocar <code>stdio_init_all()</code> y <code>printf()</code>, ya provistas por el SDK, en lugar de manipular registros de silicio directamente como ocurre en las prácticas de GPIO o PWM. Esta decisión ilustra, precisamente, el propósito de la capa de ensamblador dentro del curso: mostrar el mecanismo de llamada de bajo nivel que subyace a cualquier función del SDK, sin necesidad de reimplementar el USB completo.</p><h2 id="hardware-y-conexiones" tabindex="-1">Hardware y Conexiones <a class="header-anchor" href="#hardware-y-conexiones" aria-label="Permalink to &quot;Hardware y Conexiones&quot;">​</a></h2><p>Esta práctica no requiere conexiones externas. La comunicación se realiza mediante el puerto <strong>USB-C integrado</strong> de la placa RP2040.</p><table tabindex="0"><thead><tr><th>Conexión</th><th>Descripción</th></tr></thead><tbody><tr><td>USB-C (placa RP2040)</td><td>Conexión directa a la PC; expone un puerto serial virtual (CDC-ACM)</td></tr></tbody></table><h2 id="configuracion-del-proyecto-—-cmake" tabindex="-1">Configuración del Proyecto — CMake <a class="header-anchor" href="#configuracion-del-proyecto-—-cmake" aria-label="Permalink to &quot;Configuración del Proyecto — CMake&quot;">​</a></h2><div class="language-cmake vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cmake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">pico_enable_stdio_usb(18_uarthelloworld 1)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">pico_enable_stdio_uart(18_uarthelloworld 0)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">target_link_libraries</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(18_uarthelloworld</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    pico_stdlib</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>La habilitación de <code>pico_enable_stdio_usb</code> en 1 es la que enruta <code>printf</code> hacia el USB en tiempo de enlace; <code>pico_enable_stdio_uart</code> se deshabilita explícitamente en 0 para evitar que el mismo flujo de <code>stdio</code> se duplique hacia la UART física.</p><h2 id="codigo-fuente-—-main-c" tabindex="-1">Código Fuente — <code>main.c</code> <a class="header-anchor" href="#codigo-fuente-—-main-c" aria-label="Permalink to &quot;Código Fuente — \`main.c\`&quot;">​</a></h2><div class="language-asm vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/**</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * @file   main.c</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * @author obviousfancy</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * @board  pico</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * @sdk    Raspberry Pi Pico SDK 2.2.0</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> */</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.syntax unified</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.cpu cortex-m0plus</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.thumb</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">section .rodata</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mensaje:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .asciz &quot;Hello World desde USB\\n&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">section .text</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">.global</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> main</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.thumb_func</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">main:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @ Inicializa stdio sobre USB. La enumeracion CDC ante el</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @ host la resuelve el stack TinyUSB integrado en el SDK</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @ no </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">es</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> reproducible en ensamblador puro.</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    bl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    stdio_init_all</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @ Espera para permitir que el host termine de enumerar</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @ el dispositivo USB antes del primer printf. Sin esta</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @ espera, los primeros mensajes se pierden.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ldr   r0, =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2000</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    bl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    sleep_ms</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bucle:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ldr   r0, =mensaje</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    bl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    printf</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ldr   r0, =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1000</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    bl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    sleep_ms</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    b     bucle</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.end</span></span></code></pre></div><h2 id="analisis-del-codigo" tabindex="-1">Análisis del Código <a class="header-anchor" href="#analisis-del-codigo" aria-label="Permalink to &quot;Análisis del Código&quot;">​</a></h2><ul><li><strong><code>.syntax unified</code> / <code>.cpu cortex-m0plus</code> / <code>.thumb</code>:</strong> directivas del ensamblador GNU que fijan la sintaxis unificada de ARM y el juego de instrucciones Thumb del Cortex-M0+, sin las cuales el ensamblador no podría validar ni codificar correctamente las instrucciones siguientes.</li><li><strong><code>.section .rodata</code> / <code>mensaje: .asciz ...</code>:</strong> reserva la cadena en la sección de solo lectura, terminada en NUL (<code>.asciz</code>), tal como espera <code>printf</code>.</li><li><strong><code>.global main</code> / <code>.thumb_func</code>:</strong> exponen el símbolo <code>main</code> para que el enlazador lo ubique como punto de entrada del programa, y marcan explícitamente que se trata de una función en modo Thumb (necesario para que el bit menos significativo de la dirección se fije en 1, como exige la arquitectura al saltar entre modos).</li><li><strong><code>bl stdio_init_all</code>:</strong> primera llamada real a una función de C desde ensamblador. No requiere cargar ningún argumento en <code>r0</code>, ya que la función no recibe parámetros.</li><li><strong><code>ldr r0, =2000</code> / <code>bl sleep_ms</code>:</strong> ejemplo de paso de un único argumento por <code>r0</code> (el valor en milisegundos), ilustrando la convención AAPCS descrita en el Concepto Teórico.</li><li><strong><code>ldr r0, =mensaje</code> / <code>bl printf</code>:</strong> se carga en <code>r0</code> la dirección de la cadena (no su contenido) y se invoca <code>printf</code>, que internamente reconoce que su primer argumento es un puntero a cadena de formato.</li><li><strong><code>bucle:</code> / <code>b bucle</code>:</strong> a diferencia de un <code>while(1)</code> en C, aquí el bucle infinito se expresa como una instrucción de salto incondicional (<code>B</code>) que regresa a la etiqueta <code>bucle</code>, repitiendo indefinidamente el envío del mensaje cada segundo.</li></ul><h2 id="verificacion" tabindex="-1">Verificación <a class="header-anchor" href="#verificacion" aria-label="Permalink to &quot;Verificación&quot;">​</a></h2><p>Una vez flasheado el programa mediante <code>pico-flash</code> (SWD), se debe abrir una terminal serial (por ejemplo <code>pico-monitor.py</code> o cualquier monitor de puerto serie) apuntando al puerto virtual USB-CDC que expone la placa. Tras la espera inicial de 2 segundos, debe observarse el siguiente mensaje repitiéndose cada segundo:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Hello World desde USB</span></span>
<span class="line"><span>Hello World desde USB</span></span>
<span class="line"><span>Hello World desde USB</span></span></code></pre></div><h2 id="errores-comunes-y-variantes" tabindex="-1">Errores Comunes y Variantes <a class="header-anchor" href="#errores-comunes-y-variantes" aria-label="Permalink to &quot;Errores Comunes y Variantes&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Error común</th><th>Causa</th><th>Solución</th></tr></thead><tbody><tr><td>No aparece ningún mensaje en la terminal</td><td><code>pico_enable_stdio_usb</code> no está en 1, o falta el <code>target_link_libraries</code> con <code>pico_stdlib</code></td><td>Verificar el <code>CMakeLists.txt</code> del proyecto</td></tr><tr><td>Se pierden los primeros mensajes</td><td>El host no terminó de enumerar el dispositivo USB antes del primer <code>printf</code></td><td>Aumentar el retardo inicial (<code>sleep_ms</code>) tras <code>stdio_init_all</code></td></tr><tr><td>El programa no arranca / queda en <code>fault</code></td><td>Falta la directiva <code>.thumb_func</code> sobre <code>main</code>, y el enlazador no fija correctamente el bit Thumb de la dirección</td><td>Confirmar la presencia de <code>.thumb_func</code> inmediatamente antes de la etiqueta</td></tr><tr><td>Caracteres corruptos o líneas cortadas en la terminal</td><td>Configuración de fin de línea del monitor serial no coincide con <code>\\n</code></td><td>Configurar el monitor en modo LF o CRLF según corresponda</td></tr></tbody></table><p><strong>Variantes propuestas:</strong></p><ol><li>Modificar el mensaje para que incluya un contador que se incremente en cada iteración del bucle, pasando el valor como segundo argumento de <code>printf</code> (<code>r1</code>).</li><li>Reemplazar el retardo fijo de 1000 ms por un valor cargado desde una constante <code>.equ</code>, y experimentar con distintos intervalos.</li><li>Agregar una segunda cadena en <code>.rodata</code> y alternar entre ambas en cada iteración del bucle, usando un registro como bandera de selección.</li></ol>`,24)])])}const m=a(o,[["render",l]]);export{h as __pageData,m as default};
