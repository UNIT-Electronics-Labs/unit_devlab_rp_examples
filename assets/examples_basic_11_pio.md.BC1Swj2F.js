import{_ as e}from"./chunks/adcpractice4.VoyyGTnB.js";import{_ as s,o as i,c as n,a2 as o}from"./chunks/framework.HMsj64NP.js";const l="/unit_devlab_rp_examples/assets/pio_diagram.DgbNroqh.svg",g=JSON.parse('{"title":"PIO: Máquinas de Estado","description":"","frontmatter":{},"headers":[],"relativePath":"examples/basic/11_pio.md","filePath":"examples/basic/11_pio.md"}'),p={name:"examples/basic/11_pio.md"};function d(t,a,c,r,h,k){return i(),n("div",null,[...a[0]||(a[0]=[o('<h1 id="pio-maquinas-de-estado" tabindex="-1">PIO: Máquinas de Estado <a class="header-anchor" href="#pio-maquinas-de-estado" aria-label="Permalink to &quot;PIO: Máquinas de Estado&quot;">​</a></h1><p>Esta práctica cierra la sección introduciendo el Programmable I/O (PIO), la característica más distintiva del RP2040: un conjunto de pequeñas máquinas de estado programables mediante un juego de instrucciones propio, capaces de implementar periféricos completos en lo que equivale a &quot;hardware definido por software&quot;, sin intervención continua de ningún núcleo de CPU. A modo de cierre, esta práctica recrea el parpadeo del LED de la práctica de Blink, pero delegando por completo el control del tiempo a una máquina de estados PIO, en lugar del propio procesador.</p><h2 id="concepto-teorico" tabindex="-1">Concepto Teórico <a class="header-anchor" href="#concepto-teorico" aria-label="Permalink to &quot;Concepto Teórico&quot;">​</a></h2><p>El RP2040 cuenta con dos bloques PIO, cada uno con cuatro máquinas de estado independientes y una memoria compartida de apenas 32 instrucciones. El juego de instrucciones es deliberadamente reducido —nueve instrucciones en total (<code>JMP</code>, <code>WAIT</code>, <code>IN</code>, <code>OUT</code>, <code>PUSH</code>, <code>PULL</code>, <code>MOV</code>, <code>IRQ</code>, <code>SET</code>)—, pero su combinación permite construir protocolos de comunicación completos o patrones de temporización precisos. Cada máquina de estados dispone de dos registros de trabajo (<code>X</code>, <code>Y</code>), y se comunica con el programa en C que se ejecuta en el procesador mediante dos FIFOs de hardware —una de entrada y otra de salida—, de manera similar a como Core0 y Core1 se comunicaban en la práctica de Multicore, aunque aquí uno de los dos extremos no es un núcleo de CPU, sino una máquina de estados dedicada.</p><p>El programa empleado en esta práctica recibe, una sola vez, un valor de retardo a través de la FIFO (mediante <code>pull</code>), lo conserva en el registro <code>Y</code>, y a partir de ahí enciende y apaga el pin del LED de manera indefinida, empleando <code>X</code> como contador regresivo para cada mitad del ciclo —recargándolo desde <code>Y</code> en cada vuelta, ya que <code>X</code> se agota durante el conteo—. A diferencia de <code>sleep_ms()</code>, empleado en Blink, aquí el retardo no depende de que el procesador esté disponible para atenderlo: la máquina de estados cuenta ciclos de manera autónoma, incluso si ambos núcleos están ocupados con otra tarea.</p><p>El siguiente diagrama muestra, por separado, la configuración que ocurre en C (una sola vez, al arrancar) y el programa PIO que corre de manera continua e independiente dentro de la máquina de estados:</p><div align="center"><img src="'+l+`" width="600px" alt="Configuracion en C y programa PIO autonomo para el parpadeo del LED"></div><p><strong>Cálculo del valor de retardo.</strong> Cada mitad del ciclo (LED encendido o apagado) consume <code>Y + 3</code> ciclos del reloj del sistema: uno para <code>mov x, y</code>, uno para <code>set pins</code>, y <code>Y + 1</code> para el bucle <code>jmp x--</code> (que se ejecuta una vez por cada valor de <code>X</code> desde <code>Y</code> hasta <code>0</code>, inclusive). Un ciclo completo (encendido + apagado) consume por tanto:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>periodo_en_ciclos = 2 × (Y + 3)</span></span>
<span class="line"><span>frecuencia = clk_sys / periodo_en_ciclos = clk_sys / (2 × (Y + 3))</span></span></code></pre></div><p>Despejando <code>Y</code> para obtener una frecuencia de parpadeo de 1 Hz —la misma cadencia empleada en Blink—, con el reloj de sistema típico de 125 MHz:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Y = clk_sys / (2 × frecuencia) − 3 = 125 000 000 / 2 − 3 = 62 499 997</span></span></code></pre></div><p>Este es, precisamente, el valor que el código de esta práctica envía a la máquina de estados a través de la FIFO.</p><h2 id="hardware-y-conexiones" tabindex="-1">Hardware y Conexiones <a class="header-anchor" href="#hardware-y-conexiones" aria-label="Permalink to &quot;Hardware y Conexiones&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Elemento</th><th>Pin del RP2040</th><th>Descripción</th></tr></thead><tbody><tr><td>LED integrado</td><td>GPIO25</td><td>Mismo LED de la práctica de Blink; ahora controlado por una máquina de estados PIO</td></tr></tbody></table><h2 id="configuracion-del-proyecto-cmake" tabindex="-1">Configuración del Proyecto (CMake) <a class="header-anchor" href="#configuracion-del-proyecto-cmake" aria-label="Permalink to &quot;Configuración del Proyecto (CMake)&quot;">​</a></h2><p>A diferencia de las prácticas anteriores, un programa PIO requiere un paso adicional en el CMakeLists —más allá de <code>target_link_libraries</code>— para que el ensamblador de PIO (<code>pioasm</code>) traduzca el archivo <code>.pio</code> a un encabezado de C durante la compilación:</p><div class="language-cmake vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cmake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">target_link_libraries</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">\${PROJECT_NAME}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    pico_stdlib</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    hardware_pio</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">pico_generate_pio_header(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">\${PROJECT_NAME}</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> \${CMAKE_CURRENT_LIST_DIR}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/blink.pio)</span></span></code></pre></div><h2 id="codigo-fuente" tabindex="-1">Código Fuente <a class="header-anchor" href="#codigo-fuente" aria-label="Permalink to &quot;Código Fuente&quot;">​</a></h2><p>Esta práctica se compone de dos archivos: el programa PIO (<code>blink.pio</code>) y el código en C que lo carga y lo pone en marcha.</p><p><code>blink.pio</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>; blink.pio</span></span>
<span class="line"><span>; Enciende y apaga un pin durante un numero de ciclos recibido por la FIFO</span></span>
<span class="line"><span></span></span>
<span class="line"><span>.program blink</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    pull block          ; Espera el valor de retardo enviado desde C</span></span>
<span class="line"><span>    out y, 32            ; Lo guarda en Y (se conserva durante toda la practica)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>.wrap_target</span></span>
<span class="line"><span>    mov x, y             ; Recarga el contador regresivo desde Y</span></span>
<span class="line"><span>    set pins, 1          ; Enciende el LED</span></span>
<span class="line"><span>lp1:</span></span>
<span class="line"><span>    jmp x-- lp1          ; Permanece encendido durante (Y + 1) ciclos</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    mov x, y             ; Recarga el contador regresivo desde Y</span></span>
<span class="line"><span>    set pins, 0          ; Apaga el LED</span></span>
<span class="line"><span>lp2:</span></span>
<span class="line"><span>    jmp x-- lp2          ; Permanece apagado durante (Y + 1) ciclos</span></span>
<span class="line"><span>.wrap                    ; Regresa al inicio del bucle, indefinidamente</span></span>
<span class="line"><span></span></span>
<span class="line"><span>% c-sdk {</span></span>
<span class="line"><span>static inline void blink_program_init(PIO pio, uint sm, uint offset, uint pin) {</span></span>
<span class="line"><span>    pio_gpio_init(pio, pin);</span></span>
<span class="line"><span>    pio_sm_set_consecutive_pindirs(pio, sm, pin, 1, true);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    pio_sm_config config = blink_program_get_default_config(offset);</span></span>
<span class="line"><span>    sm_config_set_set_pins(&amp;config, pin, 1);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    // El divisor de reloj de la maquina de estados se deja en su valor por</span></span>
<span class="line"><span>    // defecto (1.0, velocidad maxima); esta practica regula la frecuencia</span></span>
<span class="line"><span>    // unicamente mediante el valor de retardo contado por software.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    pio_sm_init(pio, sm, offset, &amp;config);</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span>%}</span></span></code></pre></div><p><code>Practice_PIO_12.c</code>:</p><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/**</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@file</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> main.c</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@brief</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> Parpadeo del LED integrado mediante una maquina de estados PIO</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> *</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@author</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> obviousfancy</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * @board  pico</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * @sdk    Raspberry Pi Pico SDK 2.2.0</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> */</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* ─── Includes ─────────────────────────────────────────── */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;pico/stdlib.h&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;hardware/pio.h&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;hardware/clocks.h&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;blink.pio.h&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* ─── Defines ──────────────────────────────────────────── */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LED_PIN</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 25</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* ─── Main ─────────────────────────────────────────────── */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    PIO pio </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> pio0;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    uint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sm </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    uint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> offset </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> pio_add_program</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(pio, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">blink_program);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    blink_program_init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(pio, sm, offset, LED_PIN);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    pio_sm_set_enabled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(pio, sm, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // Valor de retardo para 1 Hz, con clk_sys = 125 MHz (ver Concepto Teorico)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    uint32_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> delay_value </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">clock_get_hz</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(clk_sys) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    pio_sm_put_blocking</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(pio, sm, delay_value);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    while</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // El nucleo queda libre: el parpadeo lo sostiene la maquina de</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // estados PIO de manera completamente autonoma.</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        tight_loop_contents</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h2 id="analisis-del-codigo" tabindex="-1">Análisis del Código <a class="header-anchor" href="#analisis-del-codigo" aria-label="Permalink to &quot;Análisis del Código&quot;">​</a></h2><p>En <code>blink.pio</code>, <code>pull block</code> detiene la máquina de estados hasta que el programa en C deposite un valor en la FIFO; <code>out y, 32</code> lo transfiere al registro <code>Y</code>. Dentro de <code>.wrap_target</code>, cada mitad del ciclo recarga <code>X</code> desde <code>Y</code> (<code>mov x, y</code>), fija el nivel del LED (<code>set pins</code>), y consume <code>Y + 1</code> ciclos adicionales mediante el bucle <code>jmp x--</code>, tal como se detalló en el Concepto Teórico; <code>.wrap</code> regresa al inicio sin el costo de una instrucción <code>jmp</code> explícita. El bloque <code>c-sdk</code> embebido en el propio archivo <code>.pio</code> define <code>blink_program_init()</code>, una función auxiliar en C que prepara el pin (<code>pio_gpio_init</code>, <code>pio_sm_set_consecutive_pindirs</code>) y construye la configuración de la máquina de estados (<code>sm_config_set_set_pins</code>) antes de inicializarla; el comentario sobre el divisor de reloj documenta una opción disponible (<code>sm_config_set_clkdiv</code>) pero no utilizada, ya que esta práctica regula la frecuencia únicamente mediante el conteo por software.</p><p>En <code>Practice_PIO_12.c</code>, <code>pio_add_program()</code> carga el programa ensamblado en la memoria de instrucciones del bloque PIO y retorna el desplazamiento (<em>offset</em>) en el que quedó ubicado —necesario porque varios programas pueden convivir en la misma memoria compartida—. <code>blink_program_init()</code> y <code>pio_sm_set_enabled(pio, sm, true)</code> dejan la máquina de estados lista y en marcha. <code>pio_sm_put_blocking()</code> deposita en la FIFO el valor de retardo calculado a partir del reloj del sistema real (<code>clock_get_hz(clk_sys)</code>), en lugar de asumir 125 MHz de manera fija. El ciclo principal en C queda vacío: a diferencia de Blink, aquí el propio procesador no participa en absoluto en la temporización del parpadeo.</p><h2 id="verificacion" tabindex="-1">Verificación <a class="header-anchor" href="#verificacion" aria-label="Permalink to &quot;Verificación&quot;">​</a></h2><p>El LED integrado debe encender y apagar a intervalos de un segundo, exactamente como en la práctica de Blink —la diferencia no es visual, sino de diseño: aquí el parpadeo continúa de manera autónoma aunque el procesador esté ocupado con cualquier otra tarea, o incluso detenido.</p><div align="center"><img src="`+e+'" width="300px" alt="LED integrado parpadeando mediante PIO"><p><em>Estado esperado del LED integrado durante la práctica</em></p></div><h2 id="errores-comunes-y-variantes" tabindex="-1">Errores Comunes y Variantes <a class="header-anchor" href="#errores-comunes-y-variantes" aria-label="Permalink to &quot;Errores Comunes y Variantes&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Síntoma</th><th>Causa típica</th></tr></thead><tbody><tr><td>El LED nunca enciende</td><td>Falta <code>pio_sm_put_blocking()</code>; la máquina de estados permanece detenida en <code>pull block</code> esperando el valor de retardo</td></tr><tr><td>Error de compilación relacionado con <code>blink.pio.h</code></td><td>Falta <code>pico_generate_pio_header()</code> en el CMakeLists, o el archivo <code>.pio</code> no coincide con el nombre indicado ahí</td></tr><tr><td>El parpadeo ocurre a una velocidad inesperada</td><td>Cálculo incorrecto de <code>delay_value</code>, o uso de un valor fijo de <code>clk_sys</code> que no corresponde a la frecuencia real de la placa</td></tr></tbody></table><p><strong>Variantes:</strong></p><ul><li>Enviar un nuevo valor de retardo por la FIFO cada pocos segundos desde el ciclo principal en C, para variar la velocidad del parpadeo sin detener la máquina de estados.</li><li>Cambiar <code>sm_config_set_clkdiv()</code> a un valor mayor que 1.0 y recalcular <code>delay_value</code> en consecuencia, para lograr la misma frecuencia con un valor de retardo mucho menor.</li><li>Lanzar una segunda máquina de estados sobre otro pin, con un valor de retardo distinto, y observar cómo ambas parpadean de manera independiente sin coordinación adicional.</li></ul>',33)])])}const E=s(p,[["render",d]]);export{g as __pageData,E as default};
