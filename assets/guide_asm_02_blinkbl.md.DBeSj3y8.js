import{_ as a,o as s,c as i,a2 as n}from"./chunks/framework.HMsj64NP.js";const o="/unit_devlab_rp_examples/assets/blink_bl_diagram.Cg8TyqJM.svg",u=JSON.parse('{"title":"Ensamblador: Blink por Llamadas BL al SDK","description":"","frontmatter":{},"headers":[],"relativePath":"guide/asm/02_blinkbl.md","filePath":"guide/asm/02_blinkbl.md"}'),l={name:"guide/asm/02_blinkbl.md"};function d(r,e,t,c,p,h){return s(),i("div",null,[...e[0]||(e[0]=[n(`<h1 id="ensamblador-blink-por-llamadas-bl-al-sdk" tabindex="-1">Ensamblador: Blink por Llamadas BL al SDK <a class="header-anchor" href="#ensamblador-blink-por-llamadas-bl-al-sdk" aria-label="Permalink to &quot;Ensamblador: Blink por Llamadas BL al SDK&quot;">​</a></h1><p>Esta practica retoma el parpadeo del LED integrado (GPIO25) de la practica anterior, pero reemplaza el acceso directo a los registros de silicio por llamadas mediante la instruccion <code>BL</code> a las funciones oficiales del Pico C/C++ SDK: <code>gpio_init</code>, <code>gpio_set_dir</code>, <code>gpio_put</code> y <code>sleep_ms</code>. El proposito es contrastar de forma directa ambos enfoques: el mismo resultado observable (un LED parpadeando), logrado en un caso escribiendo registros y en el otro delegando esa responsabilidad al SDK.</p><p><code>gpio_set_dir</code> y <code>gpio_put</code> no se invocan de forma directa desde <code>main.s</code>, sino a traves de dos wrappers (<code>asm_gpio_set_dir</code>, <code>asm_gpio_put</code>) definidos en un archivo auxiliar, <code>sdk.c</code>, que acompana a esta practica. La razon se explica en el Concepto Teorico.</p><h2 id="concepto-teorico" tabindex="-1">Concepto Teorico <a class="header-anchor" href="#concepto-teorico" aria-label="Permalink to &quot;Concepto Teorico&quot;">​</a></h2><p>La instruccion <code>BL</code> (Branch with Link) realiza dos operaciones en un solo paso: guarda en el registro <code>LR</code> (Link Register) la direccion de la instruccion siguiente a la propia <code>BL</code>, y salta a la direccion indicada. Esto permite que la funcion invocada, al terminar, regrese al punto exacto desde el que fue llamada mediante la instruccion <code>BX LR</code>. Es el mecanismo fundamental que hace posible la subrutina en el conjunto de instrucciones ARM.</p><p>El paso de argumentos entre el codigo llamador y la funcion invocada sigue la convencion de llamada estandar de ARM (AAPCS - ARM Architecture Procedure Call Standard): los primeros cuatro argumentos enteros o punteros se colocan en los registros <code>r0</code>, <code>r1</code>, <code>r2</code> y <code>r3</code>, en ese orden. El valor de retorno, si lo hay, se recibe en <code>r0</code>. Ninguna de las funciones utilizadas en esta practica requiere mas de dos argumentos, por lo que basta con <code>r0</code> y <code>r1</code>.</p><p>Desde la perspectiva del ensamblador, cada una de estas funciones del SDK es una caja negra: el codigo que aqui se escribe no necesita conocer que registro de <code>SIO</code> o de <code>IO_BANK0</code> se modifica internamente. Esa es precisamente la logica que se implemento a mano en la practica anterior, y que ahora queda oculta detras de la interfaz en C del SDK. Esta es la esencia de la progresion pedagogica de tres capas del curso: cada capa abre la caja negra de la anterior, y esta practica representa el punto en el que esa caja se cierra de nuevo para dar paso a la capa central del curso (Pico C/C++ SDK).</p><p><code>gpio_set_dir</code> y <code>gpio_put</code> estan declaradas como <code>static inline</code> en el header <code>hardware/gpio.h</code> del SDK. Una funcion inline no genera un simbolo de enlazado (linker symbol): el compilador de C inserta el codigo directamente en el sitio de la llamada, algo que un archivo de ensamblador puro (<code>.S</code>) no puede replicar, ya que solo puede saltar (<code>BL</code>) a una direccion con nombre. Por eso, invocarlas de forma directa desde <code>main.s</code> falla con <code>undefined reference</code>. La solucion es <code>sdk.c</code>, un archivo auxiliar en C que expone dos wrappers reales -- <code>asm_gpio_set_dir</code> y <code>asm_gpio_put</code> -- que unicamente invocan a las versiones inline del SDK; el compilador si puede expandirlas ahi porque se trata de un contexto de compilacion C. <code>gpio_init</code> y <code>sleep_ms</code> no requieren wrapper porque ya son funciones reales (no inline) del SDK.</p><h2 id="hardware-y-conexiones" tabindex="-1">Hardware y Conexiones <a class="header-anchor" href="#hardware-y-conexiones" aria-label="Permalink to &quot;Hardware y Conexiones&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Señal</th><th>Pin fisico</th><th>Notas</th></tr></thead><tbody><tr><td>LED integrado</td><td>GPIO25</td><td>LED soldado en la placa, no requiere conexion externa</td></tr></tbody></table><h2 id="configuracion-del-proyecto" tabindex="-1">Configuracion del Proyecto <a class="header-anchor" href="#configuracion-del-proyecto" aria-label="Permalink to &quot;Configuracion del Proyecto&quot;">​</a></h2><p>A diferencia de la practica anterior, aqui si se requiere enlazar la biblioteca estandar del SDK, ya que el codigo invoca directamente sus funciones de manejo de GPIO y de temporizacion.</p><div class="language-cmake vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cmake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">add_executable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(main.S sdk.c)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">target_link_libraries</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(pico_stdlib)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">pico_add_extra_outputs(main.S)</span></span></code></pre></div><p><code>sdk.c</code> se agrega como fuente adicional junto a <code>main.s</code>: es el archivo que provee los wrappers <code>asm_gpio_set_dir</code> y <code>asm_gpio_put</code> que el ensamblador necesita para invocar las funciones inline del SDK.</p><h2 id="codigo-fuente" tabindex="-1">Codigo Fuente <a class="header-anchor" href="#codigo-fuente" aria-label="Permalink to &quot;Codigo Fuente&quot;">​</a></h2><div class="language-asm vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">asm</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/**</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * @file main.s</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * @author obviousfancy</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * @board pico</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * @sdk Raspberry Pi Pico SDK 2.2.0</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> */</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.syntax unified</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.cpu cortex-m0plus</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.thumb</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">equ</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> LED_PIN,    </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">25</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">equ</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GPIO_OUT,    </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">equ</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> DELAY_MS,  </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">500</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">section .text</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">.global</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> main</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.thumb_func</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">main:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @ gpio_init(LED_PIN)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    movs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  r0, #LED_PIN</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    bl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    gpio_init</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @ asm_gpio_set_dir(LED_PIN, GPIO_OUT) -- wrapper de gpio_set_dir (inline)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    movs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  r0, #LED_PIN</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    movs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  r1, #GPIO_OUT</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    bl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    asm_gpio_set_dir</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">loop</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @ asm_gpio_put(LED_PIN, true) -- wrapper de gpio_put (inline)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    movs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  r0, #LED_PIN</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    movs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  r1, #</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    bl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    asm_gpio_put</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @ sleep_ms(DELAY_MS)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ldr   r0, =DELAY_MS</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    bl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    sleep_ms</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @ asm_gpio_put(LED_PIN, false)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    movs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  r0, #LED_PIN</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    movs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  r1, #</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    bl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    asm_gpio_put</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    @ sleep_ms(DELAY_MS)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ldr   r0, =DELAY_MS</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    bl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    sleep_ms</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    b     </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">loop</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.end</span></span></code></pre></div><h2 id="analisis-del-codigo" tabindex="-1">Analisis del Codigo <a class="header-anchor" href="#analisis-del-codigo" aria-label="Permalink to &quot;Analisis del Codigo&quot;">​</a></h2><ul><li><strong><code>bl gpio_init</code>:</strong> internamente saca de reset y configura el pad correspondiente, y selecciona la funcion <code>SIO</code> para el pin — el mismo trabajo realizado manualmente sobre <code>IO_BANK0</code> en la practica anterior, pero resuelto aqui en una sola llamada. Es una funcion real del SDK (no inline), por lo que se invoca directamente sin wrapper.</li><li><strong><code>movs r0, #LED_PIN</code> antes de cada llamada:</strong> dado que ninguna de las funciones invocadas preserva necesariamente el valor previo de <code>r0</code>, el numero de pin se vuelve a cargar explicitamente antes de cada <code>bl</code>, en lugar de asumir que permanece intacto entre llamadas.</li><li><strong><code>bl asm_gpio_set_dir</code>:</strong> wrapper definido en <code>sdk.c</code> que invoca a <code>gpio_set_dir</code> (inline en el SDK); equivale a la escritura sobre el alias <code>GPIO_OE_SET</code> del bloque <code>SIO</code> realizada en la practica anterior.</li><li><strong><code>bl asm_gpio_put</code>:</strong> wrapper definido en <code>sdk.c</code> que invoca a <code>gpio_put</code> (inline en el SDK); equivale a la escritura sobre <code>GPIO_OUT_SET</code> o <code>GPIO_OUT_CLR</code> (segun el valor de <code>r1</code>) del bloque <code>SIO</code>.</li><li><strong><code>bl sleep_ms</code>:</strong> a diferencia del retardo por conteo de ciclos de la practica anterior, esta funcion usa internamente el temporizador de hardware del RP2040, por lo que el tiempo de espera es preciso independientemente de la frecuencia de reloj del procesador. Al igual que <code>gpio_init</code>, es una funcion real del SDK y no requiere wrapper.</li><li><strong><code>ldr r0, =DELAY_MS</code>:</strong> se utiliza <code>ldr</code> en lugar de <code>movs</code> porque el valor 500 excede el rango de 8 bits que admite la codificacion inmediata de <code>movs</code>; el ensamblador coloca el valor en el pool de literales y genera una carga indirecta.</li><li><strong><code>sdk.c</code>:</strong> requerido porque <code>gpio_set_dir</code> y <code>gpio_put</code> son <code>static inline</code> en el SDK y no generan un simbolo de enlazado alcanzable desde ensamblador puro; expone <code>asm_gpio_set_dir</code> y <code>asm_gpio_put</code> como funciones reales que envuelven a las versiones inline.</li></ul><h2 id="verificacion" tabindex="-1">Verificacion <a class="header-anchor" href="#verificacion" aria-label="Permalink to &quot;Verificacion&quot;">​</a></h2><p>Al cargar el programa mediante SWD (<code>pico-flash</code>), el LED integrado en GPIO25 debe encender y apagar de forma alternada cada 500 ms, con una periodicidad estable gracias al uso del temporizador de hardware en <code>sleep_ms</code>.</p><div align="center"><img src="`+o+'" alt="Diagrama de llamadas BL desde ensamblador hacia funciones del Pico SDK"></div><h2 id="errores-comunes-y-variantes" tabindex="-1">Errores Comunes y Variantes <a class="header-anchor" href="#errores-comunes-y-variantes" aria-label="Permalink to &quot;Errores Comunes y Variantes&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Sintoma</th><th>Causa tipica</th></tr></thead><tbody><tr><td>Error de enlazado (<code>undefined reference to gpio_init</code>)</td><td>No se agrego <code>pico_stdlib</code> en <code>target_link_libraries</code></td></tr><tr><td>Error de enlazado (<code>undefined reference to asm_gpio_set_dir</code> / <code>asm_gpio_put</code>)</td><td>No se agrego <code>sdk.c</code> como fuente del ejecutable en <code>add_executable</code></td></tr><tr><td>El LED queda siempre encendido o siempre apagado</td><td>Se omitio recargar <code>r0</code>/<code>r1</code> antes de alguna llamada, o se invirtio el orden de los argumentos</td></tr><tr><td>El parpadeo es mas lento o mas rapido de lo esperado</td><td>Se modifico <code>DELAY_MS</code> sin considerar que el valor se expresa en milisegundos, no en ciclos</td></tr><tr><td>El programa no arranca en absoluto</td><td>Falta la etiqueta <code>.global main</code> o el atributo <code>.thumb_func</code>, necesario para que el enlazador marque la funcion como codigo Thumb</td></tr></tbody></table><p><strong>Variantes propuestas:</strong></p><ol><li>Reemplazar <code>sleep_ms</code> por <code>sleep_us</code> y ajustar el valor del argumento para lograr un parpadeo perceptiblemente mas rapido, observando el limite practico de percepcion visual del ojo humano.</li><li>Agregar una segunda variable de pin y repetir la secuencia de <code>gpio_init</code>/<code>gpio_set_dir</code>/<code>gpio_put</code> para controlar un LED externo en un patron alternado (uno encendido mientras el otro esta apagado).</li><li>Sustituir las llamadas a <code>gpio_put</code> por una llamada equivalente a <code>gpio_xor_mask</code> (si esta disponible en la version del SDK utilizada) y comparar el numero de instrucciones generado frente a la version con <code>gpio_put</code>.</li></ol>',25)])])}const g=a(l,[["render",d]]);export{u as __pageData,g as default};
